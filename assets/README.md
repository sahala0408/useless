# Screenshots

Drop your screenshots here using these exact filenames so the links in the
main README work:

- screenshot-1-home.png     — the home screen with the floating objects
- screenshot-2-profile.png  — an object's personality / dating profile
- screenshot-3-reveal.png   — the soulmate reveal with the compatibility ring

On Windows, press Win + Shift + S to capture part of the screen.
